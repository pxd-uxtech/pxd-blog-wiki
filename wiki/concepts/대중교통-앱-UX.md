---
type: concept
domain: "UI/인터랙션 설계"
name: "대중교통 앱 UX"
sources: ["/1484", "/1192", "/1213"]
updated: "2026-06-15"
---
# 대중교통 앱 UX

대중교통 앱의 핵심 사용 맥락은 크게 두 가지로 나뉜다. **익숙한 경로(출퇴근 등 반복 이동)**와 **낯선 경로(새로운 목적지 탐색)**이다. 이 두 시나리오에서 사용자의 정보 필요와 앱 의존도가 크게 다르기 때문에, 동일한 UI가 두 맥락 모두를 충족하기 어렵다. 카카오지하철 앱 분석에서 드러난 것처럼, 익숙한 경로에서는 즐겨찾기·알람 기능으로 충분하지만, 낯선 경로에서는 목적지 주변 역 파악, 출구 번호, 도보 방향 정보까지 필요하다.

**현재 위치 자동 감지** 기능(가장 가까운 역 자동 지정)은 사용자의 출발역 입력 부담을 크게 줄이는 핵심 편의 기능이다. 다만 이러한 기능이 존재하더라도 **아이콘이 의미를 직관적으로 전달하지 못하면** 사용자가 기능을 발견하지 못한다. 도착 예정 시간 공유(카카오톡 연동), 환승·하차 전 알람 등은 실제 이동 맥락에서 유용하게 쓰이는 기능이다.

앱 내 **정보 단절(Context Break)** 문제도 중요한 UX 이슈다. 목적지가 어느 역 근처인지 파악하려면 지도 앱으로 이동해야 하고, 하차 후 몇 번 출구로 나가야 할지는 다시 지도 앱을 확인해야 한다. 경로 이탈 감지(내릴 역을 지나쳤을 때 알림)도 현재 제공되지 않는다. 이처럼 단일 서비스 내에서 해결되지 않는 정보 공백은 사용자가 앱 간 전환과 스크린샷 등의 우회 행동을 하도록 만든다. **서비스 간 유기적 연결(카카오지하철 ↔ 카카오맵)**이 개선 방향으로 제시된다.

환승 여유 시간 표시도 개선 여지가 있다. 단순히 "2분"이라는 수치를 제시하는 대신, **여유 있음/보통/촉박** 같은 상대적 맥락 레이블을 제공하면 사용자가 이동 속도를 조절하는 데 유용한 정보가 된다.

## 핵심 내용
- 대중교통 앱은 익숙한 경로와 낯선 경로의 두 맥락을 구별하여 설계해야 한다
- 유용한 기능이라도 아이콘이 직관적이지 않으면 **발견 가능성(Discoverability)**이 떨어진다
- 하차 후 경로 안내(출구 → 도보) 정보가 누락되면 사용자는 앱 간 전환으로 우회한다
- 숫자 기반 시간 정보보다 상대적 맥락 레이블이 행동 결정에 더 유용한 경우가 있다
- 경로 이탈 감지는 사용자가 스스로 오류를 발견하는 지연을 줄이는 안전망 기능이다

## 관련 개념
- [[사용자 행동 패턴]] — 반복 이동 vs 탐색 이동의 패턴 차이
- [[컨텍스추얼 인쿼리]] — 실제 이동 상황에서의 현장 관찰 리서치
- [[어포던스]] — 아이콘·버튼이 기능을 직관적으로 암시하는 설계 원칙
- [[금융 앱 UX]] — 특정 도메인 앱의 사용 패턴 기반 UX 분석 사례
- [[버스 정보 안내판 정보 설계]] — 버스 정류장 도착 안내판 및 노선도의 정보 설계 문제
- [[모빌리티 서비스 유형과 MaaS]] — 차량 호출·공유·마이크로 모빌리티를 아우르는 MaaS 개념과 서비스 분류

## 지도 기반 버스 앱 리디자인

버스 앱의 즐겨찾기 기능은 **맥락을 반영하지 못한 정보 설계**로 인해 불편함을 유발한다. 출근·퇴근·운동 귀가 등 다양한 상황의 버스 정보가 하나의 목록에 섞이면, 지금 내가 타야 할 버스를 찾기 위해 긴 목록을 스크롤해야 한다. 폰은 현재 위치와 시간을 알고 있으므로, 앱이 맥락을 파악해 필요한 정보만 보여주는 것이 이상적이다.

**지도 기반 UI**는 이 문제를 여러 방면에서 해결한다. 현재 위치 근처의 즐겨찾기만 지도에 표시하면 불필요한 목록 스크롤이 사라진다. 위치 정보가 부정확한 경우에도 지도 이동·줌 아웃으로 다른 즐겨찾기를 쉽게 찾을 수 있어 목록 방식보다 오류 복구가 유리하다. 버스 현재 위치를 지도 위에 표시하면 몇 정거장 전이라는 추상적 정보보다 직관적으로 도착 여부를 판단할 수 있다.

**즐겨찾기의 시간대 옵션**은 동일 정류장에서 맥락이 다른 경우를 처리한다. 퇴근 시간이면 출근 버스를 흐릿하게 표시하여, 현재 맥락에 필요한 버스에만 시각적 집중이 가능하다. 정보 위계 설계에서도 디테일이 중요한데, 같은 정류장의 여러 버스를 **도착 순서**로 정렬하고, 두 번째 도착 버스는 흐릿하게 처리하며, 1분 이내 도착 시 색상 강조로 인지 비용을 줄인다.

**현재 정류장에서의 길찾기** 맥락에 집중하면 기존 범용 길찾기 앱이 제공하지 못하는 가치를 제공할 수 있다. 출발 정류장 선택 후 지도에서 목적지를 선택하면 해당 경로의 버스 전체가 도착 시각 포함하여 표시된다. 사용자가 어떤 버스를 타는 게 최선인지 바로 판단할 수 있도록, **최선의 선택 하나를 추천**하는 정보 위계 설계가 핵심이다.

## 글로벌 차량 공유 서비스와 현지화 UX

인도네시아는 공식 교통 인프라(내비게이션 없는 택시, 수동 톨 수납)가 낙후한 반면, **차량 공유 앱 생태계**는 오히려 한국보다 발달해 있다. 이 역설은 현지 이동 문제를 앱이 대체 해결하는 구조에서 비롯된다.

**목적지 입력 방식의 차이**가 핵심 UX 가치를 결정한다. 기존 택시는 목적지를 말로 설명해야 하므로 언어 장벽과 정보 비대칭이 생기고, 기사가 지역 이름만 파악한 채 출발하는 등 오류가 발생한다. Uber·Grab은 탑승 전 목적지 좌표를 앱에서 확정하므로 이런 문제가 원천 차단된다.

**브랜드 신뢰 vs. 공급 가용성의 트레이드오프**: BlueBird는 인도네시아 최대 택시 브랜드로 신뢰도가 높지만, 공항이나 시내에서 물리적으로 잡기 어렵다. Grab은 공급 차량이 압도적으로 많아 대기 시간이 짧다. **My BlueBird 앱**은 이 공급 가시성 문제를 해결한다—앱으로 주변 BlueBird 위치 확인 및 호출이 가능해 오프라인에서 놓쳤던 신뢰 브랜드를 온라인에서 접근 가능하게 만든다.

**현지 이동 문화를 반영한 서비스 설계**: GO-JEK은 오토바이가 주 교통수단인 인도네시아에서 오토바이 공유에 특화한 서비스다. Uber·Grab도 이 시장에 Uber Motor·Grab Bike를 추가했다. 가격 경쟁력 측면에서 GO-JEK은 UberX 대비 압도적으로 저렴하다. 글로벌 플랫폼이 현지 이동 패턴을 수용하지 않으면 경쟁력을 갖추기 어렵다는 것을 보여주는 사례다.

## 출처
- [내 대중교통 이용 패턴으로 본 카카오지하철 앱 살펴보기](https://story.pxd.co.kr/1484) — 2020-01-16, 위승용 uxdragon
- [지도 기반의 버스 앱 리디자인](https://story.pxd.co.kr/1192) — 2016-11-22, 無異
- [인도네시아에서 앱으로 교통비 바가지 면하기](https://story.pxd.co.kr/1213) — 2017-03-16, 알 수 없는 사용자
