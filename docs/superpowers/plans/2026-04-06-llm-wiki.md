# pxd 블로그 LLM Wiki 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Karpathy LLM Wiki 패턴으로 pxd 블로그의 개념 위키를 구축하고, Astro 정적 사이트에서 렌더링한다.

**Architecture:** `wiki/concepts/` 디렉토리에 마크다운 개념 페이지를 쌓고, WIKI_SCHEMA.md가 LLM의 ingest 규칙을 정의한다. Astro가 이 마크다운을 읽어 `/concepts/` 경로로 렌더링한다. ingest는 Claude Code 세션에서 수동 실행.

**Tech Stack:** Astro (정적 빌드), 마크다운 (개념 페이지), Python (ingest 유틸), pxd story API

---

## 파일 구조

| 파일 | 역할 |
|---|---|
| `WIKI_SCHEMA.md` | 신규. LLM ingest 규칙 + 개념 페이지 형식 정의 |
| `wiki/index.md` | 신규. 개념 페이지 목차 |
| `wiki/log.md` | 신규. ingest 이력 |
| `wiki/concepts/` | 신규 디렉토리. 개념 페이지들 |
| `scripts/ingest_priority.py` | 신규. 조회수 기준 ingest 대상 추출 |
| `src/lib/wiki.ts` | 신규. 개념 페이지 마크다운 로드 유틸 |
| `src/pages/concepts/index.astro` | 신규. 개념 목록 페이지 |
| `src/pages/concepts/[...slug].astro` | 신규. 개별 개념 페이지 렌더링 |
| `src/layouts/Base.astro` | 수정. nav에 "개념" 링크 추가 |
| `src/pages/index.astro` | 수정. 개념 위키 섹션 추가 |
| `CLAUDE.md` | 수정. wiki 관련 지침 추가 |

---

### Task 1: WIKI_SCHEMA.md 작성

**Files:**
- Create: `WIKI_SCHEMA.md`

- [ ] **Step 1: WIKI_SCHEMA.md 작성**

```markdown
# pxd 블로그 위키 스키마

## 3계층 구조

### Raw (원본)
- `raw/all_metas.json` — 전체 메타데이터
- `raw/cluster_data.json` — 주제별 클러스터링 + 조회수
- 본문: API `?urls=/1234&body=1`로 동적 fetch. 저장하지 않음.

### Wiki (LLM 생성물)
- `wiki/concepts/{개념명}.md` — 개념 페이지
- `wiki/index.md` — 목차
- `wiki/log.md` — 이력

### Schema
- `WIKI_SCHEMA.md` — 이 파일
- `CLAUDE.md` — 프로젝트 지침

## 개념 페이지 형식

파일명: 한글, 공백은 `-`로 치환. 예: `디자인-시스템.md`

```
---
type: concept
name: "{개념명}"
sources: ["/1234", "/1235"]
updated: "YYYY-MM-DD"
---
# {개념명}

{여러 글을 종합한 요약. 2~5단락. 원문 복사 금지.}

## 핵심 내용
- 핵심 포인트 1
- 핵심 포인트 2

## 관련 개념
- [[관련개념1]] — 한 줄 설명
- [[관련개념2]] — 한 줄 설명

## 출처
- [글 제목](https://story.pxd.co.kr/{id}) — YYYY-MM-DD, 저자
```

## Ingest 절차

1. `scripts/ingest_priority.py`로 대상 글 선택 (조회수 상위, 미처리)
2. API로 본문 fetch: `https://story-api.pxd.systems/article-metas?urls=/1234,/1235&body=1`
3. 본문을 읽고 판단:
   - 기존 개념 페이지에 병합 → 해당 페이지 수정, sources에 추가
   - 새 개념 필요 → 새 파일 생성
4. `wiki/index.md` 업데이트 (항목 추가/수정)
5. `wiki/log.md`에 기록
6. 관련 개념의 `[[크로스레퍼런스]]` 업데이트

## Ingest 규칙

- 원문을 그대로 복사하지 말 것. 핵심을 추출하여 종합 요약
- 여러 글이 같은 주제 → 하나의 개념 페이지에 통합
- 개념명은 한글, 구체적으로 (예: "디자인 시스템" O, "디자인" X)
- 관련 개념은 `[[위키링크]]` 형식
- 출처 섹션에 반드시 원글 링크 포함
- sources 배열에서 이미 처리된 URL은 중복 추가하지 않을 것

## 로그 형식 (wiki/log.md)

```
## [YYYY-MM-DD] ingest | 글제목1, 글제목2, ...
- 생성: 개념페이지1, 개념페이지2
- 업데이트: 기존개념페이지1
```
```

- [ ] **Step 2: 커밋**

```bash
git add WIKI_SCHEMA.md
git commit -m "docs: add WIKI_SCHEMA.md - LLM wiki structure and ingest rules"
```

---

### Task 2: Wiki 뼈대 생성

**Files:**
- Create: `wiki/index.md`
- Create: `wiki/log.md`
- Create: `wiki/concepts/.gitkeep`

- [ ] **Step 1: wiki 디렉토리 및 초기 파일 생성**

`wiki/index.md`:
```markdown
# pxd 블로그 개념 위키

LLM이 pxd 블로그 글을 읽고 정리한 개념 페이지 모음.

## 개념 목록

아직 ingest된 개념이 없습니다. `WIKI_SCHEMA.md`의 ingest 절차에 따라 글을 추가하세요.
```

`wiki/log.md`:
```markdown
# Wiki 작업 이력

## [2026-04-06] init | 위키 구조 초기화
- wiki/index.md, wiki/log.md 생성
- WIKI_SCHEMA.md 작성
```

`wiki/concepts/.gitkeep`: 빈 파일 (디렉토리 유지용)

- [ ] **Step 2: 커밋**

```bash
git add wiki/
git commit -m "feat: create wiki skeleton - index.md, log.md, concepts/"
```

---

### Task 3: Ingest 우선순위 스크립트

**Files:**
- Create: `scripts/ingest_priority.py`

- [ ] **Step 1: ingest_priority.py 작성**

`raw/cluster_data.json`에서 조회수 상위 글을 추출하고, `raw/all_metas.json`과 매칭하여 URL을 출력하는 스크립트.

```python
#!/usr/bin/env python3
"""조회수 기준 ingest 대상 추출. wiki/concepts/에서 이미 처리된 URL은 제외."""

import json
import glob
import re
import sys

def get_ingested_urls():
    """wiki/concepts/*.md에서 sources 배열의 URL들을 수집."""
    ingested = set()
    for path in glob.glob("wiki/concepts/*.md"):
        with open(path, encoding="utf-8") as f:
            content = f.read()
        match = re.search(r'sources:\s*\[([^\]]*)\]', content)
        if match:
            urls = re.findall(r'"(/\d+)"', match.group(1))
            ingested.update(urls)
    return ingested


def main():
    n = int(sys.argv[1]) if len(sys.argv) > 1 else 10

    with open("raw/cluster_data.json", encoding="utf-8") as f:
        cluster = json.load(f)

    with open("raw/all_metas.json", encoding="utf-8") as f:
        metas = json.load(f)

    # title → meta 매핑
    title_to_meta = {}
    for m in metas:
        if m.get("title"):
            title_to_meta[m["title"].strip()] = m

    ingested = get_ingested_urls()

    # 조회수 순 정렬
    cells = sorted(cluster["data"]["cellData"], key=lambda x: -x["size"])

    candidates = []
    for cell in cells:
        title = cell["text"].strip()
        meta = title_to_meta.get(title)
        if not meta:
            continue
        if meta["url"] in ingested:
            continue
        candidates.append({
            "url": meta["url"],
            "title": title,
            "views": cell["size"],
            "author": meta.get("author", ""),
            "category": meta.get("category", ""),
        })
        if len(candidates) >= n:
            break

    print(f"=== Ingest 대상 상위 {n}개 (미처리) ===\n")
    urls = []
    for i, c in enumerate(candidates, 1):
        print(f"{i}. [{c['views']:,}뷰] {c['title']}")
        print(f"   URL: {c['url']} | 저자: {c['author']} | 카테고리: {c['category']}")
        urls.append(c["url"])

    print(f"\n--- API 호출 URL ---")
    url_param = ",".join(urls)
    print(f"https://story-api.pxd.systems/article-metas?urls={url_param}&body=1")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행 확인**

```bash
cd /Users/taekie/Works/vibecoding/pxd블로그wiki
python3 scripts/ingest_priority.py 10
```

Expected: 조회수 상위 10개 글 목록 + API URL 출력

- [ ] **Step 3: 커밋**

```bash
git add scripts/ingest_priority.py
git commit -m "feat: add ingest priority script - extracts top articles by views"
```

---

### Task 4: Astro 개념 페이지 렌더링

**Files:**
- Create: `src/lib/wiki.ts`
- Create: `src/pages/concepts/index.astro`
- Create: `src/pages/concepts/[...slug].astro`
- Modify: `src/layouts/Base.astro:65-72` (nav에 "개념" 링크 추가)
- Modify: `src/pages/index.astro` (개념 위키 섹션 추가)

- [ ] **Step 1: src/lib/wiki.ts 작성**

개념 페이지 마크다운을 파싱하여 frontmatter와 HTML로 변환하는 유틸.

```typescript
import { glob } from 'astro/loaders';
import fs from 'node:fs';
import path from 'node:path';

export interface ConceptPage {
  slug: string;
  name: string;
  sources: string[];
  updated: string;
  content: string;
}

const CONCEPTS_DIR = path.join(process.cwd(), 'wiki/concepts');

function parseFrontmatter(raw: string): { frontmatter: Record<string, any>; body: string } {
  const match = raw.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);
  if (!match) return { frontmatter: {}, body: raw };

  const fm: Record<string, any> = {};
  for (const line of match[1].split('\n')) {
    const idx = line.indexOf(':');
    if (idx === -1) continue;
    const key = line.slice(0, idx).trim();
    let val = line.slice(idx + 1).trim();
    // Parse arrays like ["/1234", "/1235"]
    if (val.startsWith('[')) {
      try { val = JSON.parse(val); } catch { /* keep as string */ }
    }
    // Strip quotes
    if (typeof val === 'string' && val.startsWith('"') && val.endsWith('"')) {
      val = val.slice(1, -1);
    }
    fm[key] = val;
  }
  return { frontmatter: fm, body: match[2] };
}

function wikiLinksToHtml(body: string, allSlugs: Set<string>): string {
  return body.replace(/\[\[([^\]]+)\]\]/g, (_, name) => {
    const slug = name.replace(/[\s/\\:]/g, '-').replace(/-+/g, '-');
    if (allSlugs.has(slug)) {
      return `<a href="/concepts/${slug}">${name}</a>`;
    }
    return `<span class="missing-link">${name}</span>`;
  });
}

export function getConceptPages(): ConceptPage[] {
  if (!fs.existsSync(CONCEPTS_DIR)) return [];

  const files = fs.readdirSync(CONCEPTS_DIR).filter(f => f.endsWith('.md'));
  const pages: ConceptPage[] = [];

  for (const file of files) {
    const raw = fs.readFileSync(path.join(CONCEPTS_DIR, file), 'utf-8');
    const { frontmatter, body } = parseFrontmatter(raw);
    pages.push({
      slug: file.replace('.md', ''),
      name: frontmatter.name || file.replace('.md', '').replace(/-/g, ' '),
      sources: Array.isArray(frontmatter.sources) ? frontmatter.sources : [],
      updated: frontmatter.updated || '',
      content: body,
    });
  }

  return pages.sort((a, b) => a.name.localeCompare(b.name, 'ko'));
}

export function renderConceptContent(content: string, allSlugs: Set<string>): string {
  // Convert [[wiki links]] to HTML
  let html = wikiLinksToHtml(content, allSlugs);

  // Basic markdown to HTML (headers, lists, links, bold, paragraphs)
  html = html
    .replace(/^### (.+)$/gm, '<h3>$1</h3>')
    .replace(/^## (.+)$/gm, '<h2>$1</h2>')
    .replace(/^# (.+)$/gm, '<h1>$1</h1>')
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
    .replace(/\[([^\]]+)\]\(([^)]+)\)/g, '<a href="$2" target="_blank" rel="noopener">$1</a>')
    .replace(/^- (.+)$/gm, '<li>$1</li>')
    .replace(/(<li>.*<\/li>\n?)+/g, (m) => `<ul>${m}</ul>`)
    .replace(/\n\n/g, '</p><p>')
    .replace(/^(?!<[hul])/gm, (line) => line ? `<p>${line}` : '')
    ;

  return html;
}
```

- [ ] **Step 2: src/pages/concepts/index.astro 작성**

```astro
---
import Base from '@/layouts/Base.astro';
import { getConceptPages } from '@/lib/wiki';

const pages = getConceptPages();
---

<Base title="개념 위키">
  <h1>개념 위키</h1>
  <p>LLM이 pxd 블로그 글을 읽고 정리한 개념 페이지 {pages.length}개</p>

  {pages.length === 0 ? (
    <p>아직 ingest된 개념이 없습니다.</p>
  ) : (
    <table>
      <thead><tr><th>개념</th><th>출처 수</th><th>업데이트</th></tr></thead>
      <tbody>
        {pages.map(p => (
          <tr>
            <td><a href={`/concepts/${p.slug}`}>{p.name}</a></td>
            <td>{p.sources.length}</td>
            <td class="date">{p.updated}</td>
          </tr>
        ))}
      </tbody>
    </table>
  )}
</Base>
```

- [ ] **Step 3: src/pages/concepts/[...slug].astro 작성**

```astro
---
import Base from '@/layouts/Base.astro';
import { getConceptPages, renderConceptContent } from '@/lib/wiki';

export function getStaticPaths() {
  const pages = getConceptPages();
  return pages.map(p => ({
    params: { slug: p.slug },
    props: p,
  }));
}

const { name, sources, updated, content } = Astro.props;
const allSlugs = new Set(getConceptPages().map(p => p.slug));
const html = renderConceptContent(content, allSlugs);
---

<Base title={name}>
  <article>
    <Fragment set:html={html} />
  </article>
  <p class="date" style="margin-top: 2rem;">최종 업데이트: {updated} | 출처 {sources.length}개</p>
  <style>
    article h1 { font-size: 1.5rem; margin-bottom: 1rem; }
    article h2 { font-size: 1.2rem; margin-top: 2rem; margin-bottom: 0.75rem; }
    article h3 { font-size: 1.05rem; margin-top: 1.5rem; margin-bottom: 0.5rem; }
    article p { margin: 0.5rem 0; }
    article ul { padding-left: 1.5rem; list-style: disc; margin: 0.5rem 0; }
    article li { padding: 0.2rem 0; }
    .missing-link { color: #9ca3af; text-decoration: underline dotted; }
  </style>
</Base>
```

- [ ] **Step 4: Base.astro nav에 "개념" 링크 추가**

`src/layouts/Base.astro`의 nav 부분 (line 65-72)에서:

변경 전:
```html
  <nav>
    <a href="/" class="site-title">pxd 블로그 위키</a>
    <a href="/overview">개요</a>
    <a href="/categories">카테고리</a>
    <a href="/authors">저자</a>
    <a href="/tags">태그</a>
    <a href="/years">연도별</a>
  </nav>
```

변경 후:
```html
  <nav>
    <a href="/" class="site-title">pxd 블로그 위키</a>
    <a href="/concepts">개념</a>
    <a href="/overview">개요</a>
    <a href="/categories">카테고리</a>
    <a href="/authors">저자</a>
    <a href="/tags">태그</a>
    <a href="/years">연도별</a>
  </nav>
```

- [ ] **Step 5: index.astro에 개념 위키 섹션 추가**

`src/pages/index.astro` 상단에 import 추가:
```typescript
import { getConceptPages } from '@/lib/wiki';
const conceptPages = getConceptPages();
```

`<h1>` 바로 아래, 기존 `<p>` 뒤에 개념 위키 섹션 추가:
```astro
  {conceptPages.length > 0 && (
    <>
      <h2><a href="/concepts">개념 위키</a> <span class="count">({conceptPages.length})</span></h2>
      <p>LLM이 블로그 글을 읽고 정리한 개념 페이지</p>
      <ul>
        {conceptPages.slice(0, 10).map(p => (
          <li><a href={`/concepts/${p.slug}`}>{p.name}</a> <span class="count">출처 {p.sources.length}개</span></li>
        ))}
        {conceptPages.length > 10 && <li><a href="/concepts">전체 보기 →</a></li>}
      </ul>
    </>
  )}
```

- [ ] **Step 6: 빌드 확인**

```bash
source ~/.nvm/nvm.sh && nvm use 22
cd /Users/taekie/Works/vibecoding/pxd블로그wiki
npx astro build
```

Expected: 기존 135개 페이지 + `/concepts/index.html` 추가 (개념 페이지 0개이므로 개별 페이지 없음). 빌드 성공.

- [ ] **Step 7: 커밋**

```bash
git add src/lib/wiki.ts src/pages/concepts/ src/layouts/Base.astro src/pages/index.astro
git commit -m "feat: add concept wiki pages - Astro rendering for wiki/concepts/*.md"
```

---

### Task 5: CLAUDE.md 업데이트

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: CLAUDE.md에 wiki 섹션 추가**

기존 `CLAUDE.md`의 `## 규칙` 섹션 뒤에 추가:

```markdown

## LLM Wiki (Karpathy 패턴)

이 프로젝트는 Karpathy의 LLM Wiki 패턴을 적용합니다.
- 구조 규칙: `WIKI_SCHEMA.md` 참조
- 개념 페이지: `wiki/concepts/*.md`
- ingest 우선순위: `python3 scripts/ingest_priority.py 10`

### Ingest 실행 방법
1. `python3 scripts/ingest_priority.py 10` → 대상 글 + API URL 확인
2. API URL로 본문 fetch
3. 본문을 읽고 `wiki/concepts/`에 개념 페이지 생성/업데이트
4. `wiki/index.md`, `wiki/log.md` 업데이트
5. 커밋
```

- [ ] **Step 2: 커밋**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md with LLM wiki ingest instructions"
```

---

### Task 6: 첫 번째 Ingest 실행 (검증)

**Files:**
- Create: `wiki/concepts/{생성될 개념}.md` (LLM이 결정)
- Modify: `wiki/index.md`
- Modify: `wiki/log.md`

- [ ] **Step 1: ingest 대상 확인**

```bash
cd /Users/taekie/Works/vibecoding/pxd블로그wiki
python3 scripts/ingest_priority.py 5
```

조회수 상위 5개 글의 URL과 API 호출 URL을 확인.

- [ ] **Step 2: API로 본문 fetch**

출력된 API URL을 WebFetch로 호출하여 본문 수신.

- [ ] **Step 3: 개념 페이지 생성**

WIKI_SCHEMA.md의 형식에 따라 `wiki/concepts/`에 개념 페이지 생성.
LLM이 본문을 읽고:
- 어떤 개념으로 묶을지 판단
- 종합 요약 작성
- 관련 개념 `[[위키링크]]` 연결
- 출처 섹션에 원글 링크 포함

- [ ] **Step 4: wiki/index.md 업데이트**

생성된 개념 페이지를 목차에 추가:
```markdown
## 개념 목록

- [개념명](concepts/개념명.md) — 한 줄 요약
```

- [ ] **Step 5: wiki/log.md 업데이트**

```markdown
## [2026-04-06] ingest | 글제목1, 글제목2, ...
- 생성: 개념페이지1, 개념페이지2
```

- [ ] **Step 6: 빌드 + 확인**

```bash
source ~/.nvm/nvm.sh && nvm use 22
npx astro build
npx astro preview --port 4321
```

`/concepts/` 페이지에서 생성된 개념 페이지가 렌더링되는지 확인.

- [ ] **Step 7: 커밋**

```bash
git add wiki/ 
git commit -m "feat: first wiki ingest - add concept pages from top-viewed articles"
```
