{
  "name": "pxd-blog-wiki",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npx pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.3.0",
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.0"
  }
}
